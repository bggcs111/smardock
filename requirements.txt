# ===== 交互层 =====
gradio>=4.44.0
# gr.Dataframe 依赖 pandas Styler，需要较新的 jinja2（避免 ImportError）
jinja2>=3.1.5

# ===== 存储层 =====
chromadb>=0.4.22

# ===== 云端 AI 能力 =====
dashscope>=1.20.0        # 向量化（text-embedding-v3）
openai>=1.30.0           # 问答 Agent（DeepSeek 兼容 OpenAI 协议）

# ===== 解析层 =====
pymupdf>=1.24.0          # PDF 解析（版式几何、图片位置）
pymupdf4llm>=1.24.0      # PDF 版式模型 → Markdown（默认引擎，自带标题层级与列表结构）
python-docx>=1.1.0       # Word 解析

# ===== 配置 =====
python-dotenv>=1.0.0

# ===== 本地 NER（隐私模式的 PII 识别）=====
# 人名/地名/机构名识别；未安装时 PIIRedactor 自动降级为「仅正则脱敏」。
# 取消下面的注释，然后删除.venv\.deps-ok，重新运行start.bat，即可启用本地NER。
modelscope>=1.20.0
torch>=2.0.0
transformers>=4.50.0,<5     # 5.x 移除了 transformers.onnx，modelscope CRF 模型不兼容
huggingface-hub>=0.34.0,<1.0
datasets>=2.19.0
addict
simplejson
ruamel.yaml
sortedcontainers
